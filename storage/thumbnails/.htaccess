# Cinematic Studio Family — TODO #37
# storage/thumbnails/ — Anzeige-Zugriff erlaubt (generierte Thumbnail-Bilder).
# Kein Directory-Listing, kein CGI/PHP-Execution.

Require all granted
Options -Indexes -ExecCGI

<FilesMatch "\.(php|phtml|phar)$">
    Require all denied
</FilesMatch>
